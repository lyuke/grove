# Grove — macOS 轻量项目编辑器

> Grove 意为「树林」，寓意把多个项目集中在一处管理。

- 创建日期：2026-09-15
- 状态：第一版功能与双架构试用包已交付；Intel 实机和长期工作负载验证待完成，详见 [VERIFY.md](VERIFY.md)
- 定位：面向 macOS 的轻量 VS Code
- 平台：Apple Silicon（arm64）与 Intel（x64）

## 一、已确认的产品方向

- 使用 Electron 构建桌面应用。
- 以文件浏览和代码编辑为核心，同时提供项目管理、终端和 Git 能力。
- 第一版只管理本地目录。
- 在底部真实终端直接运行现有 Agent CLI，不单独开发 Agent 对话界面。
- 本地优先、单人使用，第一版不涉及云同步和团队协作。

## 二、第一版范围

| 模块       | 第一版功能                                         |
| ---------- | -------------------------------------------------- |
| 项目管理   | 添加本地目录、搜索、排序、修改显示名称、从列表移除 |
| 文件树     | 展开目录，新建、重命名、移动和删除文件或文件夹     |
| 编辑器     | 多标签编辑、语法高亮、查找替换、保存、未保存提示   |
| 项目搜索   | 按文件名快速打开、全文搜索                         |
| 终端       | 底部多终端、运行本机 Shell 和 Agent CLI、中断任务  |
| Git        | 分支展示、变更列表、Diff、暂存、取消暂存、提交     |
| 工作区恢复 | 恢复项目、打开的文件、面板布局和编辑位置           |
| macOS 体验 | 原生菜单、常用快捷键、深浅主题、双架构安装包       |

移除项目默认只移除列表记录。终端进程在切换项目时保留；退出应用后，第一版不恢复原进程。

暂不包含插件系统、调试器、完整语言服务、远程开发、Git Clone 和专门的 Agent 聊天界面。

## 三、界面结构

```text
┌──────────────────────────────────────────────────────────┐
│ 标题栏：当前项目 / 当前分支 / 快速打开                    │
├──────────┬──────────────┬─────────────────────────────────┤
│ 项目列表 │ 文件 / 搜索  │ 文件标签栏                      │
│          │ / Git 变更   ├─────────────────────────────────┤
│ 项目 A   │              │ 代码编辑器 / Diff               │
│ 项目 B   │              │                                 │
│ 项目 C   │              ├──────── 拖动调整高度 ───────────┤
│          │              │ 终端工具栏：字号 / 新建 / 最大化│
│          │              │ Shell / Agent CLI               │
├──────────┴──────────────┴─────────────────────────────────┤
│ 状态栏：文件类型 / 编码 / 行列位置 / Git 状态             │
└──────────────────────────────────────────────────────────┘
```

项目列表和文件侧栏可折叠、拖动调整宽度。终端位于编辑区下方，可拖动上边缘调整高度，支持最大化/还原与字体缩放；布局状态自动保存。

## 四、技术架构

- **桌面框架：** Electron。
- **界面：** React + TypeScript。
- **编辑器与 Diff：** Monaco Editor。
- **终端：** xterm.js + node-pty。
- **Git：** 调用本机 Git，以结构化结果驱动界面。
- **全文搜索：** ripgrep。
- **状态存储：** 本地保存设置、项目列表和工作区状态。

职责分为三层：

1. **界面层：** 面板、编辑器、终端展示和交互。
2. **工作区层：** 管理项目、打开的文件、未保存内容和会话归属。
3. **系统服务层：** 文件操作、目录监听、搜索、Git 和终端进程。

每次文件、终端和 Git 操作都明确绑定项目，避免切换项目后操作错目录。UI 通过受控接口访问系统服务。

## 五、实施计划

### 阶段 1：桌面骨架与项目管理

- 初始化应用和开发、构建流程。
- 实现可调整布局、项目列表和目录选择。
- 保存项目配置，处理路径失效与重复添加。
- 尽早验证两种架构的原生依赖打包。

**验收：** 添加多个本地项目，切换与重启恢复正常，双架构应用均能启动。

### 阶段 2：文件编辑闭环

- 实现文件树和基础文件操作。
- 接入 Monaco、多标签、保存和快捷键。
- 实现快速打开、全文搜索。
- 监听外部修改，处理文件被删除、改名及内容冲突。

**验收：** 能浏览、搜索、编辑真实项目；Agent 修改文件时，未保存内容不会被直接覆盖。

### 阶段 3：终端集成

- 接入真实 Shell，支持多终端与尺寸调整。
- 每个终端绑定项目目录。
- 支持直接运行已安装的 Agent CLI。
- 处理环境变量、中文输入、长输出和进程退出。
- 关闭运行中终端或退出应用时提示。

**验收：** 从 Finder 启动应用后，也能使用配置好的开发命令；交互式 CLI 正常工作，切换项目不影响任务。

### 阶段 4：Git 闭环

- 展示工作区、暂存区和未跟踪文件。
- 支持文件级 Diff、暂存和取消暂存。
- 实现提交信息输入、提交执行和失败反馈。
- 对二进制文件、非 Git 目录和冲突状态提供明确展示。

**验收：** 在应用内完成编辑、检查 Diff、暂存、提交；提交钩子失败时保留输入并显示原因。

### 阶段 5：打包与日常使用验证

- 完善菜单、主题、快捷键和状态恢复。
- 检查大型目录、多个终端和长时间运行表现。
- 分别产出 Apple Silicon 与 Intel 安装包。
- 在两种架构上验证文件编辑、PTY 和 Git 完整流程。

**验收：** 两种架构均完成实际运行验证；若缺少对应测试设备，明确记录未验证项。

## 六、关键实现原则

- **编辑可靠性优先：** 明确区分磁盘内容与未保存内容，关闭和外部修改都不能造成静默丢失。
- **大项目按需加载：** 文件树不一次性扫描所有内容，搜索和监听合理排除依赖与构建目录。
- **界面保持响应：** 搜索、Git 和终端输出异步处理。
- **提交范围清晰：** 提交暂存区内容，让用户能在提交前确认。
- **双架构从一开始验证：** 尤其关注 PTY 等原生依赖，不把兼容性留到最后。

## 七、第一版交付标准

阶段 1–4 完成核心功能，阶段 5 达到可日常使用。

完整工作流：

> 选择项目 → 浏览文件 → 编辑代码 → 运行 Agent CLI → 检查 Git Diff → 暂存 → 提交。

## 八、后续可扩展方向

- 语言服务、代码补全和跳转。
- Git 历史、分支操作、拉取、推送、冲突解决和 worktree。
- Agent 启动快捷入口、任务历史和任务管理。
- 插件系统、调试器和远程开发。

以上扩展不作为第一版验收要求。

## 九、执行结果（2026-09-16）

- [x] 阶段 1：应用骨架、项目管理和状态保存。
- [x] 阶段 2：文件树、编辑器、搜索及外部修改冲突处理。
- [x] 阶段 3：多项目真实 PTY 终端和现有 CLI 运行入口。
- [x] 阶段 4：工作区/暂存区 Diff、暂存、取消暂存和提交。
- [x] 阶段 5：菜单、主题、快捷键、布局恢复、双架构 DMG 构建。
- [x] 10 项系统测试；底部终端高度、字体缩放、最大化及布局恢复；修复编辑器拖选文本时选区被折叠的问题。
- [x] 源码构建 6 项端到端测试；安装包验证结果见 VERIFY.md。
- [ ] Intel 实机验证。
- [ ] 长时间真实工作负载及中文输入法等人工体验验证。

安装包位于 `release/0.1.4/`。使用方式见 [README.md](README.md)，验证范围和限制见 [VERIFY.md](VERIFY.md)。当前版本没有 Apple Developer ID 签名或公证。

## 十、日常使用完善（0.1.2）

- [x] 原生文件菜单增加“全部保存”（⌘ ⌥ S），覆盖所有项目已打开文件。
- [x] 退出时支持保存并退出、不保存并退出和取消。
- [x] 文件冲突或保存失败时阻止退出，保留未保存内容。
- [x] 退出前写入最新布局、标签和光标状态，避免等待中的设置写入丢失。
- [x] 新增退出生命周期端到端测试；源码、arm64 安装包、x64 安装包（Rosetta）各 8 项通过，10 项系统测试及双架构 DMG 完整性检查通过。

## 十一、启动与浏览性能（0.1.3）

- [x] 异步加载登录 Shell 环境，使窗口与文件查看无需等待 Shell 配置完成。
- [x] 文件树与单行组件缓存渲染，减少输入、光标移动和标签切换时的更新。
- [x] 编辑器稳定配置与监听回调，去掉重复全文读取。
- [x] 终端渲染按需加载、编辑器空闲预加载。
- [x] 增加可重复运行的性能基准，记录同机双版本安装包的三轮测试结果，详见 PERFORMANCE.md。

- [x] 0.1.3：10 项系统测试、arm64 安装包 9 项端到端测试、x64（Rosetta）8 项功能测试及双架构 DMG 校验通过；转译环境跳过 1 项启动时序测试。

## 十二、Diff 与终端完善（0.1.4）

- [x] Git 状态请求合并与失效缓存，关闭只读查询的可选索引写入，减少重复 Git 进程。
- [x] Diff 复用编辑器和模型，切换文件时忽略过期请求；折叠未修改区域，大文件关闭语法着色。
- [x] 修复终端可用高度计算和小数行高导致的滚动不同步，保证最后一行可见；调整大小时保留底部跟随状态，新增滚动到底部按钮。
- [x] 默认 Control + 反引号打开、聚焦或收起终端；支持自定义快捷键并同步原生菜单。
- [x] Command + 点击终端 HTTP/HTTPS 链接，通过默认浏览器打开。
- [x] 终端可拖到右侧或下方，支持两方向尺寸调整及最大化；移动时保留 PTY 会话。

验证结果和性能原始数据见 VERIFY.md 与 PERFORMANCE.md。
