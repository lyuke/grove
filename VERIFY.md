# Grove 0.1.4 验证记录

验证日期：2026-09-17。测试主机：Apple Silicon，macOS / Darwin 25.6.0。

## 构建与检查

| 检查                                    | 结果                                                     |
| --------------------------------------- | -------------------------------------------------------- |
| TypeScript 严格检查（含未使用变量检查） | 通过                                                     |
| Prettier 格式检查                       | 通过                                                     |
| 文件系统、真实 Git 仓库与快捷键测试     | 14 项通过                                                |
| 0.1.0：开发服务器浏览器检查             | 页面有内容，无错误覆盖层；普通浏览器显示桌面应用启动提示 |
| 0.1.0：Electron 连接 Vite 开发服务器    | 5 项端到端测试通过                                       |
| Apple Silicon 最终应用                  | 11 项端到端测试通过                                      |
| Intel 最终应用（Rosetta）               | 10 项功能测试通过，启动时序测试跳过                      |
| arm64 / x64 DMG 完整性                  | 均通过 `hdiutil verify`                                  |

所有端到端测试均使用临时 Git 项目和独立应用数据目录。未修改用户已有项目或应用配置。

0.1.4 Apple Silicon 最终安装包 11 项端到端测试通过；Intel（Rosetta）9 项功能测试在整套回归中通过，大文件 Diff 在调整等待上限后单独复跑通过，共 10 项功能测试通过，1 项启动时序测试因转译初始化超出测试的 Shell 等待上限而跳过；双架构 DMG 完整性校验通过。性能基准及环境限制见 [PERFORMANCE.md](PERFORMANCE.md)。

## 端到端覆盖

1. 鼠标拖选、键盘扩展选区及替换文本；打开文件、修改、保存；模拟外部 Agent 修改文件；比较冲突、保留编辑器版本并保存；全文搜索；查看 Diff、暂存并提交；真实 PTY 输入输出；深浅主题。
2. 添加及去重项目、修改显示名称、新建和重命名文件、项目间终端隔离、界面重载恢复、移除项目但保留磁盘文件。
3. 提交钩子拒绝提交时，显示错误并保留提交信息与暂存文件。
4. 读取包含 1,200 个文件的目录并搜索；同时保留两个终端；连续输出 10,000 行；打开 TypeScript 文件；检查 1000 × 680 窗口布局。
5. 完全退出测试应用后重新启动，恢复打开的文件、活动标签和主题；不恢复旧终端进程。

6. 终端默认位于编辑区下方；拖动上边缘调整高度；字体放大、缩小和重置；最大化、还原及收起/展开；界面重载后保留布局设置，同一 PTY 会话继续运行。

7. 多文件全部保存；取消退出；外部文件冲突导致保存失败时保持应用打开；处理冲突后保存并退出。
8. 不保存并退出时保持磁盘内容；关闭干净窗口无需确认；修改终端字号后立即退出仍保存最新布局，随后重新启动恢复。

0.1.1 回归过程中修正了测试时序：冲突对话框关闭后等待并聚焦编辑器再保存；重载前等待第二个项目的重命名标签写入设置文件。Apple Silicon 与 Intel（Rosetta）最终安装包各 6 项完整回归通过。

编辑器选区问题已通过新增拖选测试复现：光标位置回写会重复调用 `setPosition`，将选区折叠。修复为仅在目标位置与当前光标不同时设置位置，该问题在后续版本中继续进行回归验证。

9. 慢登录 Shell 配置下，窗口和文件读取先于环境准备完成；Shell 就绪后验证主进程与终端命令路径。该时序测试只在原生架构执行。

10. 大文件 Diff 复用同一个编辑器；快速切换只显示最新选择，外部修改自动刷新，并排/行内切换保留实例。
11. 自定义终端快捷键与原生按键事件；输出、换位置和字号调整后底部可见；滚动到底部；拖动停靠保留会话；Command + 点击链接；非法协议拒绝；重载后设置保留。

0.1.4 Rosetta 回归中，Git 提交测试增加明确等待暂存及编辑器切换完成，再填写并检查提交信息，避免自动化输入与异步视图切换重叠。大文件 Diff 功能断言允许等待 15 秒，以覆盖 Rosetta 首次初始化 Monaco 工作线程的波动；性能数据另由原生安装包基准记录，不能以功能测试通过推断 Intel 实机延迟。

## 系统服务覆盖

- UTF-8 BOM 与 CRLF 保留；磁盘版本变化后拒绝旧版本保存。
- 拒绝越过项目根目录的路径和指向外部的符号链接。
- 新建和移动文件时不覆盖已有目标。
- 拒绝二进制和无效 UTF-8 文本编辑。
- 重命名符号链接时保留目标文件。
- 首次提交前暂存和取消暂存。
- 分别读取暂存区和工作区 Diff。
- Git 重命名、删除和仓库子目录路径。
- 将 Git pathspec 特殊字符作为普通文件名处理。
- 正确区分普通目录和 Git 仓库。
- 只读 Git 状态检查不改写索引；复用已知文件状态读取 Diff。
- 终端快捷键规范化、冲突检查及物理按键映射。

## 安装包

最终文件位于 `release/0.1.4/`。该目录与早期构建分开，避免覆盖已运行的应用。

| 文件                    | SHA-256                                                            |
| ----------------------- | ------------------------------------------------------------------ |
| `Grove-0.1.4-arm64.dmg` | `d761f78dd2929a248e699c3d51de9658f039331fbb392d6209a64fe29a926634` |
| `Grove-0.1.4-x64.dmg`   | `e3cdacb906814de8e811a512398c31d2c820274f2886e545b4a0f3271b1ac819` |

两种安装包分别包含对应架构的 Electron、node-pty 和 ripgrep。ripgrep 下载时通过固定的官方 SHA-256 校验，许可文件随应用附带。

## 验证边界与待办

- Intel 版本在 Apple Silicon 主机上通过 Rosetta 运行；尚无 Intel 实机验证。
- 已完成有限目录规模和终端输出量的检查，尚未完成数小时至数天的真实工作负载验证。
- 安装包没有 Apple Developer ID 签名和公证，适合作为本地试用构建。
- Monaco 0.52 的同名符号高亮在快速切换模型时出现未处理的取消错误，因此暂时关闭该功能；语法高亮和查找保持可用。升级 Monaco 后可重新评估。
- 文件删除到 macOS 废纸篓、中文输入法组合输入及交互式 Git 签名仍需日常人工体验验证。

复现命令见 [README.md](README.md)。截图位于 `artifacts/`。
