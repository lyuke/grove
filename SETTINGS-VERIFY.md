# 设置与历史提交版本验证

2026-09-17，本机 macOS Apple Silicon。

- `npm run build`：通过。
- `npm test`：15 项通过。
- `npx playwright test`：12 项通过，包含设置持久化、历史提交、编辑、搜索、Git、PTY 和退出保护。
- arm64 安装包：启动、终端、设置和历史 3 项通过。
- x64 安装包：通过 Rosetta 验证终端、设置和历史 2 项，不代替 Intel 实机测试。

## 体积

按实际文件字节数计算（非文件系统分配大小），相对现有 0.1.4 安装包：

| 架构  | 原包 MiB | 新包 MiB |   减少 |
| ----- | -------: | -------: | -----: |
| arm64 |   132.84 |   100.47 | 24.37% |
| x64   |   138.53 |   105.27 | 24.01% |

前端依赖不再作为生产依赖重复携带，主进程压缩构建；保留 PTY 原生运行时和许可。

## 加载性能

设置和历史按需加载；只注册已有语言映射所需的 Monaco 语言；没有项目时跳过编辑器预热，有项目时保留空闲预热。

同机各 3 次、1200 文件测试，中位数：

| 指标         | 原包 ms | 新包 ms |
| ------------ | ------: | ------: |
| 启动到文件树 |   659.7 |   699.3 |
| 首次打开文件 |   439.6 |   437.0 |

此次小样本未证明启动速度有显著提升；首次打开文件基本持平。不能将依赖精简幅度当作启动加速幅度。原始数据位于 artifacts/performance-before-settings.json 和 artifacts/performance-after-settings.json；测试使用系统缓存，且期间有安装包检查，结果仅作本机观察。
